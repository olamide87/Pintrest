// need to write code that prints 4 different boards(cards) to the page 4 different div containers of equal size
// when you click on one of the boards it should hide the other three boards and only display the selected board
// when your you have single board view you should be able to see all the pins on the boards 
import './boards.scss';
import $ from 'jquery';
import firebase from 'firebase/app';
import 'firebase/auth';
import utilities from '../../helpers/utilities';
import boardData from '../../helpers/data/boardData';
import boardCard from '../BoardCard/boardCard';
import pinsData from '../../helpers/data/pinData';
import pinsPrint from '../PinCard/pinCard';


const boardsBuilder = () => {

}
